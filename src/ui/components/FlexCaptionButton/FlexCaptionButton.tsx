import React from "react";
import { FlexCaptionButtonProps } from "./FlexCaptionButton.type" 
 
 const FlexCaptionButton = ({test}: FlexCaptionButtonProps) => {
  return (
     <>{test}</>
  )
}

export default FlexCaptionButton
